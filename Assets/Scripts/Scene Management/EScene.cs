using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/// <summary>
/// An enum for denoting which scene to load without having to memorise the id of each scene.
/// </summary>
public enum EScene
{
    MainMenu = 0,
    Game = 1
}
